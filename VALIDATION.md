# گزارش بررسی نسخهٔ 1.3.0

تاریخ: 2026-09-23

| بررسی | نتیجه |
| --- | --- |
| ساخت `mobileDebug` و `tvDebug` | موفق؛ APK هر دو تولید شد |
| ساخت `mobileRelease` و `tvRelease` با R8 و کوچک‌سازی منابع | موفق؛ ساخت محلی بدون کلید خصوصی انتشار انجام شد |
| آزمون Java نسخهٔ گوشی | ۸ آزمون، صفر شکست و خطا |
| آزمون Java نسخهٔ تلویزیون | ۸ آزمون، صفر شکست و خطا |
| آزمون حرکت با ریموت | ۳ آزمون موفق: حرکت در شبکه، ورودی متن و حبس/بازگشت فوکوس پنجره |
| Android Lint گوشی و تلویزیون | صفر Error؛ هشدارهای غیرمسدودکننده باقی هستند |
| امضای APKهای Debug | بررسی با `apksigner` موفق؛ امضای v2 |
| مشخصات APK تلویزیون | لانچر Leanback، نام FilmBuff TV، بنر و عدم نیاز به صفحهٔ لمسی تأیید شد |

آزمون‌های Java، حفظ دقیق query و token لینک‌ها، مرز دامنهٔ مورد اعتماد، MIME زیرنویس‌های resolver، فایل‌های UTF-8/UTF-16/Windows-1256، تشخیص SRT/VTT/ASS و رد فایل خالی/نامعتبر/بیش‌ازحد بزرگ را بررسی می‌کنند. آزمون حرکت با ریموت روی DOM شبیه‌سازی‌شده اجرا شده است.

هشدارهای Lint عمدتاً مربوط به متن‌های ثابت فارسی، آیکن‌های به‌ارث‌رسیده، منابع استفاده‌نشده و پیشنهادهای تطبیق با نسخه‌های تازهٔ Android هستند. برای عبور از بررسی، خطاها پنهان یا غیرفعال نشده‌اند.

مواردی که هنوز نیازمند آزمایش واقعی‌اند: اجرای رابط روی گوشی و تلویزیون، کدک‌های دستگاه، انتخاب فایل از USB تلویزیون، پخش منبع زنده، اینترنت اپراتورهای ایران و امضای Release با کلید خصوصی مالک. در این محیط امولاتور/تلویزیون و اتصال ایرانی در دسترس نبودند؛ ساخت موفق جایگزین این آزمون‌ها نیست.

به‌روزرسانی 2026-09-24: workflow فقط APK و AAB امضاشدهٔ Release گوشی و تلویزیون را تحویل می‌دهد؛ هر چهار مقدار امضا از Secrets یا Variables خوانده می‌شوند و نبود تنظیمات، ساخت را متوقف می‌کند. کلید جدیدی ساخته نشده است. نتیجهٔ ساخت با کلید مالک باید از اجرای GitHub Actions تأیید شود. بررسی‌های محلی جدول بالا مربوط به ساخت قبلی همین کد برنامه‌اند.

بررسی همین به‌روزرسانی: نحو YAML و همهٔ بلوک‌های Bash معتبر؛ خواندن Secrets و Variables، توقف هنگام نبود کلید و نبود خروجی Debug تأیید شد. هر سه آزمون حرکت ریموت دوباره موفق بودند. نام تنظیمات با workflow فعلی مخزن `TinTech-VibeDev/TinTechApp` تطبیق داده شد. اتصال GitHub هنگام ثبت تغییرات با خطای 403 متوقف شد؛ هیچ commit یا اجرای Actions جدیدی ثبت نشده و APK امضاشده با کلید مالک هنوز تولید نشده است.

بستهٔ مستقل Mobile: تنظیمات ساخت و بارگذاری Artifact فقط همین نسخه را هدف می‌گیرند. نحو YAML و Bash و مسیرهای خروجی بررسی شدند؛ کد برنامه نسبت به نسخهٔ بررسی‌شده تغییر نکرده است.
