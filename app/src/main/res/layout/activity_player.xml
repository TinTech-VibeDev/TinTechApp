<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/playerRoot" android:layout_width="match_parent" android:layout_height="match_parent" android:background="#000000">
    <androidx.media3.ui.PlayerView android:id="@+id/playerView" android:layout_width="match_parent" android:layout_height="match_parent"
        android:keepScreenOn="true" app:use_controller="true" app:show_buffering="when_playing"
        app:controller_layout_id="@layout/player_controls" app:show_timeout="3500" app:surface_type="surface_view" />
    <TextView android:id="@+id/gestureHint" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_gravity="center"
        android:background="@drawable/control_focus" android:padding="20dp" android:textColor="#FFFFFF" android:textSize="22sp" android:visibility="gone" />
    <LinearLayout android:id="@+id/playerError" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_gravity="center"
        android:orientation="vertical" android:gravity="center" android:background="@drawable/control_focus" android:padding="24dp" android:visibility="gone">
        <TextView android:id="@+id/playerErrorText" android:layout_width="wrap_content" android:layout_height="wrap_content" android:textColor="#FFFFFF" android:textSize="18sp" android:gravity="center" android:layout_marginBottom="16dp" />
        <LinearLayout android:layout_width="wrap_content" android:layout_height="wrap_content">
            <Button android:id="@+id/playerRetry" style="@style/FilmBuffControl" android:text="تلاش دوباره" />
            <Button android:id="@+id/playerChangeRoute" style="@style/FilmBuffControl" android:text="مسیر اتصال" />
            <Button android:id="@+id/playerExit" style="@style/FilmBuffControl" android:text="بازگشت" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
